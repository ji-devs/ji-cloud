{
  "name": "config",
  "version": "1.0.0",
  "description": "",
  "main": "dist/config.cjs.js",
  "module": "dist/config.esm.js",
  "browser": "dist/config.umd.js",
  "scripts": {
    "build": "npm run build:tsc && rollup --config rollup.config.js",
    "build:tsc": "npm run clean && tsc",
    "clean": "rimraf ./.ts-output && rimraf ./dist"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "rollup": "^2.35.1",
    "typescript": "^4.1.2"
  }
}
